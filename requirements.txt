# Core FastAPI dependencies
fastapi
uvicorn[standard]
pydantic
python-dotenv

# HTTP requests
requests

# Vector database
pinecone-client

# Embeddings
# sentence-transformers

# Note: The following are automatically installed as dependencies:
# - torch, transformers, numpy, scipy (dependencies of sentence-transformers)
# - python-multipart (optional, only needed for file uploads)